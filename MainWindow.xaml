<Window x:Class="SmartFit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartFit"
        mc:Ignorable="d"
       x:Name="mainwindow"  Title="" Height="1024" Width="1440"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterScreen"
        Padding="2,0,2,4" Background="Transparent" MinHeight="700" MinWidth="984">

    <!-- Main Window Border -->
    <Border x:Name="MainBorder" Background="#FFFFFF" CornerRadius="50" BorderThickness="2" BorderBrush="LightGray">
        <Grid>
            <!-- Custom Rounded Title Bar -->
            <Border x:Name="TitleBar" Background="#B8D9FF" CornerRadius="50,50,0,0" Height="50" VerticalAlignment="Top"
                    MouseDown="TitleBar_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="51" />
                        <ColumnDefinition Width="73" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="SmartFit" FontSize="18" FontWeight="Bold"
                               VerticalAlignment="Center" Margin="20,0,0,0" Height="24"/>

                    <!-- Minimize Button -->
                    <Button Grid.Column="1" Content="━" FontSize="16" Click="Minimize_Click"
                            Background="Transparent" BorderBrush="Transparent" ToolTip="Minimize" Margin="0,6,0,6" HorizontalAlignment="Center" Width="38" Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
  CornerRadius="10"
  BorderBrush="Transparent"
  BorderThickness="0"
  Padding="5"
  Effect="{StaticResource DropShadowEffect}">
                                    <ContentPresenter HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>

                    <!-- Maximize/Restore Button -->
                    <Button Grid.Column="2" Content="▢" FontSize="16" Click="MaximizeRestore_Click"
                            Background="Transparent" BorderBrush="Transparent" ToolTip="Maximize/Restore" Margin="5,6,5,6" Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
  CornerRadius="10"
  BorderBrush="Transparent"
  BorderThickness="0"
  Padding="5"
  Effect="{StaticResource DropShadowEffect}">
                                    <ContentPresenter HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>

                    <!-- Close Button -->
                    <Button  Grid.Column="3" Content="✕" FontSize="16" Click="Close_Click"
                            Background="Transparent" BorderBrush="Transparent" Foreground="Red" ToolTip="Close" Margin="13,6,27,6" Cursor="Hand" >
                        <Button.Effect>
                            <DropShadowEffect/>
                        </Button.Effect>

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                    CornerRadius="10"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    Padding="5"
                    Effect="{StaticResource DropShadowEffect}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>

                    </Button>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Margin="0,50,0,0">
                <!-- UI elements go here -->

                <StackPanel HorizontalAlignment="Left" Background="#F6F7F9" Width="220"  Margin="40,0,0,10" Height="842" VerticalAlignment="Bottom">
                    <!-- Logo -->
                    <StackPanel Orientation="Horizontal" Margin="20,30,0,20"/>

                    <!-- Navigation Items -->
                    <Button Background="#4A7AB0" HorizontalAlignment="Left" Width="180" Padding="10" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <Image  HorizontalAlignment="Left" Source="Assets\Images\Vector.png"  Width="24" Height="24" Margin="0,0,60,0"/>
                            <TextBlock Text="الرئيسية" FontSize="16" FontWeight="SemiBold" Foreground="#3E3E3E" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Background="Transparent" HorizontalAlignment="Left" Width="180" Padding="10" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Assets\Images\Dumbbells 2.png" Width="24" Height="24" Margin="0,0,60,0"/>
                            <TextBlock Text="تمارينك" FontSize="16" FontWeight="SemiBold" Foreground="#3E3E3E" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Background="Transparent" HorizontalAlignment="Left" Width="180" Padding="10" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Assets\Images\Stars Minimalistic.png" Width="24" Height="24" Margin="0,0,60,0" />
                            <TextBlock Text="إنشاء خطة مع AI" FontSize="16" FontWeight="SemiBold" Foreground="#3E3E3E" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Background="Transparent" HorizontalAlignment="Left" Width="180" Padding="10" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Assets\Images\User.png" Width="24" Height="24" Margin="0,0,60,0" />
                            <TextBlock Text="ملفك الشخصي" FontSize="16" FontWeight="SemiBold" Foreground="#3E3E3E" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Grid Margin="40,0,-40,852">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="96*"/>
                        <ColumnDefinition Width="228*"/>
                        <ColumnDefinition Width="496*"/>
                        <ColumnDefinition Width="50*"/>
                        <ColumnDefinition Width="48*"/>
                        <ColumnDefinition Width="303*"/>
                        <ColumnDefinition Width="216*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Smart Fit" Grid.Column="1" Margin="0,0,495,0" Grid.ColumnSpan="2" FontSize="24" VerticalContentAlignment="Center" Foreground="#FF4A7AB0" FontFamily=""/>
                    <Image Source="Assets\Images\Logo.png" HorizontalAlignment="Left" Height="88" VerticalAlignment="Center" Width="86"/>
                </Grid>


            </Grid>
        </Grid>
    </Border>
</Window>
